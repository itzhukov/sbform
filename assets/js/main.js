console.log('main js');

require('first');

window.addEventListener('load', (event) => {
	console.info('window ready!', event)
})
/* ================================= exports ================================= */

module.exports = {

}